
@{ ViewBag.Title = "Home";
                Layout = "~/Views/Shared/_Layout.cshtml"; }


<div class="row">
    <div class="col-sm-10  mx-auto ">
        <div class="card card-signin my-5">
            <div id="home" class="card-body" style="margin-top:70px;">
                <h5 style="margin-bottom:20px;" id="quizBaslik" class="text-center"><b>Exámenes Cardiel</b></h5>


                <div class="card">
                    <div class="card-header">
                        Bienvenidos
                    </div>
                    @if (User.IsInRole("S"))
                    {
        <div class="card-body">
            <h5 class="card-title">Realiza un examen</h5>
            <p class="card-text">Realiza las preguntas que el profesor cargó para ti</p>
            @Html.ActionLink("Iniciar", "Quiz", "Quiz", null, new { @class = "btn btn-success", @id = "sinavStart" })

        </div>}
                    @if (User.IsInRole("T"))
                    {
        <div class="card-body">
            <h5 class="card-title">Cargar Preguntas</h5>
            <p class="card-text">Aqui podras cargar las preguntas para aplicar</p>
            @Html.ActionLink("Comenzar", "CrearCuestionario", "ExamenPreparar", null, new { @class = "btn btn-success" })

        </div>}
                </div>
            </div>
        </div>
    </div>
</div>
@section scripts
{
    <script src="~/Scripts/home.js"></script>
}